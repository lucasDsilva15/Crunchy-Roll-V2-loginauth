export default function Profile ({username, email}) {
    return (
        <div>
            <h1>Profile</h1>
            <p> Username: {username}</p>
            <p> E-Mail: {email}</p>
        </div>
    )
}